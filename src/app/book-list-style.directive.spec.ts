import { BookListStyleDirective } from './book-list-style.directive';

describe('BookListStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BookListStyleDirective();
    expect(directive).toBeTruthy();
  });
});
